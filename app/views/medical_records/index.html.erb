  <% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<div class="clearfix"></div>
  <div class="col-md-12 col-sm-12  ">
    <div class="x_panel">
      <div class="x_title">
        <h2>Listing Prontuários<small>Todos os prontuários</small></h2>
        <ul class="nav navbar-right panel_toolbox">
          <li><%= link_to 'Novo Prontuário', "#" %></li>
          <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
          </li>
        </ul>
        <div class="clearfix"></div>
      </div>
      <div class="x_content">
        <table class="table table-hover">
          <thead>
            <tr>
              <th><%= @mrecords.human_attribute_name("id") %></th>
              <th><%= @mrecords.human_attribute_name("patient_symptoms") %></th>
              <th><%= @mrecords.human_attribute_name("applied_treatment") %></th>
              <th>Nome do Pet</th>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @mrecords.each do |m_record| %>
              <tr>
                <td><%= m_record.id %></td>
                <th><%= m_record.patient_symptoms %></th>
                <td><%= m_record.applied_treatment %></td>
                <td><%= m_record.pet.name %></td>
                <td><%#= link_to "Show", pet_path(@pet) %></td>
                <td><%#= link_to "Edit", edit_pet_path(@pet) %></td>
                <td><%#= link_to "Delete", pet, method: :delete, data: {confirm: 'Are you sure?'} %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <%= paginate @mrecords %>
      </div>
    </div>
  </div>